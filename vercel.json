{
  "buildCommand": "npm install --include=dev && npm --prefix apps/api install --include=dev && npm --prefix apps/api run prisma:generate && npm --prefix apps/api run build && npm run build",
  "outputDirectory": "dist/public",
  "functions": {
    "api/[...all].ts": {
      "includeFiles": "apps/api/dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://invoice-analytics-mauve.vercel.app/api/$1" },
    { "source": "/((?!api)(?!.*\\.).*)", "destination": "/index.html" }
  ]
}
