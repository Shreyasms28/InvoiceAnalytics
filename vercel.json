{
  "buildCommand": "npm install && npm --prefix apps/api install && npm --prefix apps/api run build && npm run build",
  "outputDirectory": "dist/public",
  "functions": {
    "api/[...all].ts": {
      "includeFiles": "apps/api/dist/**"
    }
  },
  "routes": [
    { "src": "/((?!api)(?!.*\\.).*)", "dest": "/index.html" }
  ]
}
